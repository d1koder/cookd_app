<h1> Cookd App </h1>

<%= link_to "New Chef", new_chef_path %>

<div> chef list </div>

<% @chefs.each do | chef | %>
<ul>
    <li> <%=chef.profile.first_name + " " + chef.profile.last_name %> </li>
    
</ul>
<% end %>